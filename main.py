from tkinter import *

# Instance of Tk
interface = Tk()

# Default settings
interface.title("Miles to Kilometer Converter")
interface.minsize(width=500, height=300)

# Keep the interface open until exited
interface.mainloop()

